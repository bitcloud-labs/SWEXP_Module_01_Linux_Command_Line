{
  "name": "swexp-module-01-linux",
  "version": "1.0.0",
  "private": true,
  "description": "Forge SWEXP Module 01 — interactive Linux/shell exercises (edit solution.sh, run tests, submit).",
  "scripts": {
    "test": "bats -r labs assignments",
    "check": "bash -n labs/*/solution.sh assignments/*/solution.sh",
    "grade": "node scripts/grade.mjs"
  },
  "devDependencies": {
    "bats": "^1.13.0"
  }
}
